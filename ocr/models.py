from django.db import models
'''
from django import forms

# Create your models here.

class UploadFileModel(models.Model):
    file = models.FileField(null=True, widget=forms.TextInput(attrs={'id':'#file_loader'})
'''